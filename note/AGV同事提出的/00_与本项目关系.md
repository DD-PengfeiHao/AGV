# AGV同事提出的 · 与本项目的关系（读我）

截图讲的是 **ROS2 Nav2**（`bt_navigator` / `planner_server` / …）。

**本 Demo 实车（`192.168.18.198`）走的是仙工 Robokit TCP API**，不是自研 Nav2。公开字段与端口见：

- `note/AGV智能送货机器人API`
- 项目 `docs/26_AGV_Robokit联调就绪_无内网先验通.md`

Roboshop（你电脑下载的 **Ubuntu `.deb`**，如 `Roboshop-ubuntu-2.4.2.40.deb`）= 配车/看车工具；本机 Ubuntu 22.04 符合手册要求。联调时先连上真车再释放控制权，避免与本系统抢 `4005` 锁。
