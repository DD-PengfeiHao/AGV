# 项目完整现状快照 · 转移另一台电脑（含 TRAE 更新）

> **打包时间**：2026-08-08 14:00（CST）  
> **用途**：带走开发；**数据保全**；记录真车当前故障现象  
> **裁决**：仍为 **受控联调**，**非 Demo 就绪**（见 `docs/27`）

---

## 1. 一句话现状

- **Dev 仿真链路**：TRAE 已验证 Web API 约 22 端点 + `smoke_v02` PASS；Docker 曾在 TRAE 会话正常（本打包会话 Docker socket 不可用）。  
- **真车**：曾同网段联通（NUC `192.168.18.230` ↔ AGV `192.168.18.198`），TRAE 修复 **Robokit 报文大端** 后只读 API（1004/1007/1009/1020）曾 **PASS**。  
- **当前运行态**：`env.json` 为 **`mode=demo`**；日志显示真车 **大量超时 / No route to host**，导航多次失败（含 `bad sync byte: 0x7b`）。用户描述：**真车已连上但车坏了**。  
- **相机**：`cam_front` 仍大量 `camera_missing_frames` / `xavier_ros2_waiting`（Xavier/Basler 主链路未验收）。

---

## 2. TRAE 做了什么（已核对）

主报告：`reports/20260807_171708_TRAE接手开场_v0.2.1.md`  
交接书（TRAE 侧可能续写）：`note/20260807_TRAE接手交接书.md`（已同步 `docs/28`）  
PPT 素材：`note/团队交接/20260807_PPT素材_本周进度报告.md`

| 项 | 结果 |
|----|------|
| 读门禁/交接书 | 已读，遵守不宣称 Demo 就绪 |
| G1 Docker + smoke_v02 | PASS（TRAE 当次） |
| G2 网络 | 先 BLOCKED（.0 网段），后同网段 **PASS** |
| Robokit 字节序 bug | **根因**：客户端/Mock 用小端 `<`，真车要大端 `>`；已改 `HEADER_FMT = ">BBHIH6s"` |
| Mock 回归 | 改大端后仍 PASS（活案例：Mock≠真车） |
| 真车只读 API | 曾 PASS：位姿在 **LM7**，电量 ~56%，激光 2×520 beams，`task_status=0` |
| G3 Roboshop / G4 闭环 / G5–G7 | 未完成或未书面勾选 |
| 唯一控车方 | soft boot 不起 `agv_bridge_node`（软禁用） |

**代码确认（打包时）**：`workspace_v02` 与 `.run/workspace_v02` 的 `robokit_client.py` 均为 `HEADER_FMT = ">BBHIH6s"`。

---

## 3. 真车「坏了」——日志证据（2026-08-08）

运行时配置（勿忽略）：

```json
{"mode":"demo","agv_host":"192.168.18.198","use_sim_cameras":true}
```

`system_20260808.jsonl` 摘要：

| 现象 | 次数级 | 含义 |
|------|--------|------|
| `laser_fail` / timed out | ~125+ | Demo 轮询 1009 超时 |
| `laser_fail` / No route to host | ~172+ | 主机路由不到车（车关机/离网/网卡变了） |
| `navigate_fail` / `bad sync byte: 0x7b` | 多次 | `0x7b`=`{`，疑似收到裸 JSON 或错流，协议/端口异常 |
| `navigate_req` 6 / `navigate_res` 1 / `navigate_fail` 5 | — | 多数导航失败 |
| `agv_lock_ok` 2 | — | 曾抢到锁 |
| `camera_missing_frames` | ~2340 | cam_front 无帧（与车故障独立） |

**建议（到新电脑后先做）**：

1. 切回 Dev，避免对着坏车空转：`POST /api/env {"mode":"dev"}`  
2. 用 Roboshop 看车是否还能上线；重启车端 Robokit  
3. 再测：`ping` + `nc 19204-19207` + 只读 1004（大端客户端）  
4. 若仍 `0x7b`：抓包对比 Roboshop 报文，勿盲目 3051  

---

## 4. 门禁看板（打包时）

| 门禁 | 状态 |
|------|------|
| G1 基础运行 | ✅ TRAE 曾过；转移后须在新机复验 |
| G2 网络 | ✅ 曾过；**现疑似又断**（No route） |
| G2+ 协议（大端） | ✅ 代码已修；曾只读 PASS |
| G3 Roboshop | ⏸ |
| G4 Dashboard 真车闭环 | ❌/中断（坏车 + 导航失败） |
| G5–G7 | ⏸ |

---

## 5. 打包内容与校验

见同目录：

- `AGV_Delivery_FULLPACK_20260808.tar.gz` — 工程 + note + 镜像 tar + 日志  
- `AGV_Delivery_FULLPACK_20260808.SHA256` — 校验  
- `UNPACK_README.md` — 解压与恢复步骤  

**包含**：`AGV_Delivery_v0.2.0_20260807_0921/`（含 `.run`、`images/*.tar`、`agv_downloaded`、`reports`、`docs`）、`note/`（API 手册、Roboshop 手册、agv_downloaded、TRAE/团队交接、相机契约包等）。

**排除**：`__pycache__`、`*.pyc`、`.git`（本树本无 git）、临时文件。

---

## 6. 到新电脑后最小恢复

```bash
# 1) 校验
sha256sum -c AGV_Delivery_FULLPACK_20260808.SHA256

# 2) 解压
tar -xzf AGV_Delivery_FULLPACK_20260808.tar.gz -C ~/Pengfei.Hao   # 或你的目录

# 3) 可选载入镜像（免重建）
docker load -i AGV_Delivery_.../images/delivery-ros2-humble-gazebo-v02.tar
docker load -i AGV_Delivery_.../images/ros-humble-ros-base-jammy.tar

# 4) 从 .run 拉起（按现场脚本；常见 compose profile sim-soft）
# 5) 先切 Dev，确认 smoke_v02，再考虑真车
```

编辑约定不变：改 `workspace_v02/` → 同步 `.run/workspace_v02/`。
