# 23 相机为什么连不上 · 同事要做什么 · 系统日志怎么看

> 2026-08-07 · 验证机 `192.168.0.173` · Xavier NX（相机2）`192.168.0.225` · Demo AGV `192.168.18.198`

---

## 1. 先分清：现在「相机」是谁在出图

| 环境 | 相机 | 图像来源 | 节点 |
|------|------|----------|------|
| **开发(dev)** | **相机2 `cam_front`** | **Xavier NX 人脸工程 MJPEG** `http://192.168.0.225:8080/stream` | `xavier_cam_front_bridge` → `/camera/cam_front/image_raw` |
| **开发(dev)** | 相机1/3 `cam_left`/`cam_right` | 仿真合成彩条 | `gz_physics_proxy`（已 **skip cam_front**） |
| **Demo(实车)** | AGV | 真车导航/激光 | Robokit → `192.168.18.198` |
| **Demo(实车)** | 工业相机 left/right | 默认未接 | 同事发同名 topic 或 `camera_bridge` |

**纠正（2026-08-07）：** 开发环境相机2 **不是** 再等工业相机同事，也不是 gazebo 彩条；功能已在 Xavier NX（`ubuntu`/`ubuntu`，`192.168.0.225`）实现，推流口 `:8080/stream`。

---

## 2. Xavier 相机2 怎么开（开发联调）

```bash
# SSH
ssh ubuntu@192.168.0.225   # 密码 ubuntu

cd /mnt/jetson-ssd/data/face-system/project/python+opencv
source ./env_cuda_ssd.sh

# 优先：接好 Basler acA2440-20gm 后
#   python3 run.py → 选项 2「测试相机」
# 或本仓库已放的开发推流（无相机时也会出标注占位图，便于通路验收）：
python3 _agv_dev_stream.py
```

浏览器直看：`http://192.168.0.225:8080/`  
NUC Web：`http://192.168.0.173:19999/` → 相机2 应为 **640×480**，`transport=xavier_mjpeg`。

当前实测：Xavier **未插 Basler acA2440-20gm**（`pyrealsense2` 设备数=0）；`eth1` USB 网卡无链路，Basler GigE `192.168.0.100` 也不可达。插上 Basler acA2440-20gm 后重启 `_agv_dev_stream.py` / 菜单选项2 即切真图。

---

## 3. 工业相机同事（Jason）交付契约（cam_left / cam_right）

逻辑名冻结：

| UI | 逻辑名 | 职责 | 开发环境谁出图 |
|----|--------|------|----------------|
| 相机1 | `cam_left` | 扫码 | gz 合成（待工业相机） |
| 相机2 | `cam_front` | 人脸 + 扫码 + 目标（主） | **Xavier NX** |
| 相机3 | `cam_right` | 第三视角 + 货物 | gz 合成（待工业相机） |

left/right 每人至少发布：

```text
/camera/<name>/image_raw     sensor_msgs/Image
/camera/<name>/info          delivery_interfaces/CameraInfoLite
/camera/<name>/qr            delivery_interfaces/QrDetectionArray   # 可选但建议
```

`camera_bridge_node` 对工业相机 **仍是 stub**；`cam_front` 已由 `xavier_cam_front_bridge` 接管。

---

## 4. 开发环境要跑的节点（Web 要有图）

```text
gz_physics_proxy           # odom + cam_left/right 合成（skip cam_front）
camera_gazebo_qr           # left/right info+qr；cam_front info 由 Xavier 桥接发
xavier_cam_front_bridge    # Xavier :8080/stream → /camera/cam_front/image_raw
dashboard_node             # HTTP / snapshot
face_bridge                # 人脸（可 stub）
```

环境变量（`boot_gazebo_lite.sh` 默认已开）：

```bash
CAM_FRONT_SOURCE=xavier
FACE_JETSON_IP=192.168.0.225
XAVIER_CAM_STREAM_URL=http://192.168.0.225:8080/stream
```

诊断：

```bash
curl -s http://127.0.0.1:19999/api/diag/cameras | jq .
# cam_front.transport 应为 xavier_mjpeg；分辨率约 640x480
curl -s -o /tmp/c.jpg -w "%{http_code}\n" http://127.0.0.1:19999/api/camera/cam_front/snapshot
file /tmp/c.jpg   # JPEG 640x480
```

---

## 5. 系统日志（给 Agent / 人排障）

| 路径 | 说明 |
|------|------|
| `/var/log/delivery/system/system_YYYYMMDD.jsonl` | 系统/相机/AGV/激光事件 |
| `/var/log/delivery/face/face_bridge_YYYYMMDD.jsonl` | 人脸 |
| `GET /api/system/logs?limit=100&kind=camera` | HTTP 拉取 |
| `GET /api/diag/cameras` | 相机一键诊断 |
| Web 右上角「系统日志」「相机诊断」 | Demo 页按钮 |

关键 event：`camera_frame_first` `camera_encode_fail` `camera_missing_frames` `camera_snapshot_empty` `env_switch` `laser_fail` `navigate_*` `robokit_*`

---

## 6. 环境切换

| 模式 | 含义 |
|------|------|
| 开发(dev) | 仿真 AGV + **Xavier cam_front** + left/right 合成 + 仿真激光圈 |
| Demo(实车) | Robokit → `192.168.18.198`；导航/激光真车；相机另看 image_raw |
| 生产(prod) | 预留，未启用 |

```bash
curl -s -X POST http://127.0.0.1:19999/api/env \
  -H 'Content-Type: application/json' \
  -d '{"mode":"demo","agv_host":"192.168.18.198"}'
```
