# AGV Web V0.3 — Test Environment (NUC)

**Host:** 172.31.0.84  
**Path:** `/home/ubuntu/Pengfei.Hao/AGV/V0.3`  
**Web:** http://172.31.0.84:19999/  
**Camera:** http://172.31.0.84:19999/camera.html  

## Architecture

Everything runs inside Docker container **`delivery_gazebo_soft`** (same as dev 172.31.0.111).

```
/home/ubuntu/Pengfei.Hao/AGV/V0.3/
├── workspace_v02/     # ROS2 src + docker compose
├── agv_downloaded/  # maps, models
└── docker_data/       # logs
```

## Common Commands

```bash
cd /home/ubuntu/Pengfei.Hao/AGV/V0.3/workspace_v02

# Start
sudo docker compose -f docker/docker-compose.yml --profile sim-soft up -d

# Stop
sudo docker compose -f docker/docker-compose.yml --profile sim-soft down

# Restart dashboard only
sudo docker exec delivery_gazebo_soft pkill -f delivery_web/dashboard_node
sudo docker exec -d delivery_gazebo_soft bash -lc \
  'source /opt/ros/humble/setup.bash && source /opt/delivery_ws/install/setup.bash && \
   export ROS_DOMAIN_ID=30 DELIVERY_ENV=demo && \
   exec ros2 run delivery_web dashboard_node >> /var/log/delivery/dashboard_node.log 2>&1'

# Logs
sudo docker logs delivery_gazebo_soft --tail 50
sudo docker exec delivery_gazebo_soft tail -30 /var/log/delivery/dashboard_node.log
```

## Deployed from dev (172.31.0.111) on 2026-08-13
